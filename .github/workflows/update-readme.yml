name: AI README Updater

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write
  pull-requests: write

jobs:
  update-readme:
    # Secrets are not exposed to PRs coming from forks.
    if: github.event.pull_request.head.repo.full_name == github.repository

    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Get API diff
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          git diff "$BASE_SHA" "$HEAD_SHA" -- \
            "src/**/*.controller.ts" \
            "src/**/dto/*.ts" \
            "prisma/schema.prisma" \
            > api-diff.txt

      - name: Check for API changes
        id: api_changes
        run: |
          if [ -s api-diff.txt ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Update README with AI
        if: steps.api_changes.outputs.changed == 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: node scripts/update-readme.mjs

      - name: Commit README changes
        if: steps.api_changes.outputs.changed == 'true'
        run: |
          if git diff --quiet README.md; then
            echo "README already up to date."
            exit 0
          fi

          git config user.name "ai-readme-agent"
          git config user.email "ai-readme-agent@users.noreply.github.com"

          git add README.md
          git commit -m "docs: update README from API changes"
          git push